%h2= "Editing contact for #{@contact.name}"

%fieldset.form
  %legend
  = form_for @contact do |f|
    = render :partial => "form", :locals => {:f => f}
    %p
      = f.submit "Atualizar"

= link_to "Show", @contact
